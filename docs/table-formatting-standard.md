# Table Formatting Standard

Use tables only when readers need direct comparison across consistent fields. Keep headings short, align terminology with the glossary, and avoid dense prose inside cells.

Provide explanatory text before complex tables and ensure the same information remains understandable on narrow screens.