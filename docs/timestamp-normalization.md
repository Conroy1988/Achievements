# Timestamp Normalization

Store verification timestamps in ISO 8601 with an explicit timezone. Preserve the original displayed time when useful and avoid comparing naive local times as though they share one zone.