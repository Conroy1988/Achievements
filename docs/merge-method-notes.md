# Merge Method Notes

When documenting pull-request achievements, record the merge method when it is relevant to the claim.

Distinguish merge commits, squash merges, and rebase merges. Confirm that the pull request is actually marked as merged rather than merely closed.

Do not assume that commit count and merged pull-request count are interchangeable.