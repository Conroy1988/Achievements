# Document Naming Standard

Use lowercase kebab-case filenames that describe the document’s purpose. Avoid dates in filenames unless the content is inherently period-specific.

Prefer stable names that survive routine revisions. Renames must update inbound links and preserve discoverability through an archive or redirect note.