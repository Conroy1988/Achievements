# Retest criteria

Repeat an achievement test when:

- GitHub materially changes the relevant feature;
- official documentation changes;
- a previously reliable reproduction stops working;
- independent reports conflict with the current guidance;
- a threshold or tier appears to have changed;
- the existing evidence is too old to support a current claim.

Do not retest merely to generate repository activity. Every retest should answer a defined verification question.